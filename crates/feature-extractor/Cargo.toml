[package]
name = "feature-extractor"
version = "0.1.0"
edition = "2021"
description = "Graph-based credit feature extraction from transaction networks"

[dependencies]
petgraph = "0.6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
approx = "0.5"
